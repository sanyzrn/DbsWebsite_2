/**
 * Point/grain decorative layers (hero paper grain, 404 atmosphere warp).
 * Off by default for the cleaner modern look — set to true to re-enable.
 */
export const DECORATIVE_PARTICLE_EFFECTS_ENABLED = false;
