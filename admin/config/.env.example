DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=portfolio_admin
DB_USER=portfolio
DB_PASS=change-me

# Fine-grained PAT: Contents read/write on THIS repo only
GITHUB_TOKEN=
GITHUB_OWNER=sanyzrn
GITHUB_REPO=DbsWebsite_2
GITHUB_BRANCH=main
GITHUB_CONTENT_BRANCH=content-update
PUBLISH_MODE=direct

# Optional: fire after successful publish if CI is not watching the branch
VERCEL_DEPLOY_HOOK_URL=

# Public HTTPS base for uploaded images (no trailing slash)
IMAGE_PUBLIC_BASE_URL=https://example.com/admin/uploads

# App URL used for redirects (no trailing slash)
APP_URL=https://example.com/admin
SESSION_NAME=sz_admin_sess
